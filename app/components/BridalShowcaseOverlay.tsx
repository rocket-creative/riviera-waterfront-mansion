/**
 * BridalShowcaseOverlay — thin alias kept for import compatibility.
 * All logic and config now lives in AnnouncementOverlay.tsx.
 * Edit ANNOUNCEMENT_CONFIG in that file to change the announcement.
 */
export { default } from './AnnouncementOverlay';
